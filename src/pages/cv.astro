---
import Base from '../layouts/Base.astro';
import CvContent from '../components/CvContent.astro';

/* The in-site CV: full site chrome plus a back link. The content itself
   lives in CvContent.astro, shared verbatim with /resume (the standalone,
   shareable version) so the two can't drift apart. */
---

<Base
  title="CV — Leigh Scholten"
  description="Product Design Director — two decades of high-impact digital product work across fintech and financial services."
>
  <main class="cv">
    <a href="/" class="cv__back" data-reveal>← back</a>

    <CvContent />
  </main>
</Base>

{/* CV component styles live in global.css (── CV page ── section) so the
    /styleguide page can reuse them — one source of truth, no drift. */}
